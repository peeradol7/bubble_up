import 'package:flutter/widgets.dart';

class EditPersonData extends StatelessWidget {
  const EditPersonData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
